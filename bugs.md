- [x] Currently the research percentage completion should be surfaced to the top level so you can see it from the colony screen because right now I do "Move guy off research, click research screen to observe percent, decide if that's too much or too little, then click back to teh colony screen to pick another guy and click the research screen". That data should be available from the colony screen. It would save a lot of focus to surface that. — FIXED: the colony screen's toolbar now carries a live research readout (field → target, banked/listed RP, %, +RP/turn, ~turns) computed from the PLANNED state, so it updates the moment you drag a citizen — no screen flipping. `data-testid="research-readout"` in Spreadsheet.svelte.
- [x] Why does "fix food" put everyone on research. Fix food should fix food, pulling people out of their jobs, but keep the colony-wide ratios of worker to industry as similar to how it was after the foood fixed (eg if there are 2 workers for every researcher then 2 should be pulled from work to food for every one pulled  from research.) — FIXED: `fixFoodJobs` no longer resets colonies to the research pose. It remembers each colony's worker:scientist share before the fix, redistributes the freed hands by that share, and the farmer draft pulls from whichever job is over its share. Regression test: tests/unit/foodjobs.test.ts ("keeps the worker:scientist ratio").
- [x] Have special icons or visibility for android workers and android farmers and android scientists, and ensure they are subterrainian/extra compact to place on planets just like the game rules. Ensure that robots cannot change jobs since they are that particular type forever. They need production to stay spry and healthy unlike other beings that may need food (or cybernetic or rocks) — FIXED (ENGINE_VERSION 0.15.0): android_farmers/workers/scientists are now real buildable projects (120 PP, unlocked by cybertechnics). Each completion adds a hardwired race −2 unit: +3 in its category on a no-bonus colonist's coefficient, immune to morale/gravity, eats 1 production instead of food, generates no income, never grows. Compact subterranean housing: androids never count against the organic population cap and live in their own compartments (2 per planet size). Jobs are locked (set_jobs rejects any rewiring), they never board freighters/transports, and they self-destruct when a colony is captured. UI: 🤖 icons with a teal glow, not draggable. Tests: tests/unit/androids.test.ts.
- [x] Ensure the bot ai is a very intelligent. — IMPROVED via 4 more tournament rounds (bugs/tournament/LOG.md rounds 4–7): the bot now floors its fleet at 1.25×colonies whenever it is at war AND outgunned (round 4's unconditional floor homogenized the personalities and was reverted), and the expander personality fields a real defensive navy (fleetRatio 0.5→0.7 — it eliminated the rusher in testing instead of being free food). Industrialist +108 avg score at t500; conclusions per round up from 1 to 3. Full both-seatings validation tournament in bugs/tournament/results-final.*.
- [x] Have a mode where the player is only managing research and ships and map view stuff (eg what to colonize, etc) and just has 5 slider bars (colony infrastructure, colony population, research, colony ships, military ships and transports). — FIXED: 🎚 autopilot on the colony screen. Five sliders (🏗 infrastructure, 👥 population, 🔬 research, 🚀 colony ships, ⚔ military incl. transports at high values) drive a per-turn governor (src/ui/governor.ts) that issues ordinary logged set_jobs/set_build_queue/buy_production commands — you keep research picks, ship movement, colonization targets and battles. Settings persist across reloads. Test: tests/protocol/governor.test.ts.
- [x] There should be a new realtime mode where turn timers are set to seconds (maybe 10 or 30 or 60) and start ticking down immediately. People can still commit faster but they can't wait past the timer. This mode differs from the last-person timer since this one starts right away. — FIXED: "⏱ Realtime turns" in the lobby (10/30/60s). The host arms the clock the moment each planning turn opens and force-advances at the deadline regardless of commits (committing everyone still advances early); the countdown reaches every client through the existing commit_status deadline and shows as a banner. Tests: tests/protocol/autoturn.test.ts ("realtime turns").
- [x] Please add the ability to preview the upcoming tech tree clicking on the category name. That should show all the technologies that are researched in that category. — FIXED: category names on the Research screen are clickable; a panel unfolds listing EVERY field of that category in research order with its cost, status (✓ done / 🔬 current / ● available) and all applications (known ones ✓-dimmed, hover for effects).
