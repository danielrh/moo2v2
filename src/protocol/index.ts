// Protocol: multiplayer lockstep — host sequencing, message catalog, GameSession facade.
// May import from @engine only (plus the vendored lobbylink client via the transport adapter).

export const PROTOCOL_VERSION = 1;
